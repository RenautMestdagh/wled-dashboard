# Server Configuration
PORT=3000

# Database Configuration
DB_PATH=/data/database.db

# Security Configuration
# Replace with your actual API keys, comma-separated if multiple
API_KEYS=your-secure-api-key-123,backup-key-456

# Environment
# Set to "development" in development environments
NODE_ENV=production

# CORS Configuration for Development (allow all origins)
ALLOWED_ORIGIN=*